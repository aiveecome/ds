<?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mashen.ssh.dao.IUserDao">
	
	
	<!-- 使用insert标签，插入emp对象 -->
	<insert id="add" parameterType="com.mashen.ssh.model.User">
		insert into user
		(userName) 
		values(#{userName})
	</insert>
	
</mapper>